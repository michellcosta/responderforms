name = "responderforms"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

# D1: crie em dash.cloudflare.com > Workers & Pages > D1 > Create
# Depois: Settings > Functions > D1 database bindings > DB → responderforms-db
